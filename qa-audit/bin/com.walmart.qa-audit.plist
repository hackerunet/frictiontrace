<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  launchd agent for the three-a-day QA pipeline.

  launchd rather than crontab, for two reasons that matter on a laptop:
  a missed cron entry while the machine sleeps is simply lost, whereas launchd
  runs the job when the machine wakes; and launchd keeps the job's own stdout/stderr
  even when the wrapper cannot write its log.

  Install:   bash bin/install-schedule.sh install
  Remove:    bash bin/install-schedule.sh uninstall
  Status:    launchctl list | grep qa-audit
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.walmart.qa-audit</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__PROJECT_DIR__/bin/cron-run.sh</string>
  </array>

  <key>WorkingDirectory</key>
  <string>__PROJECT_DIR__</string>

  <key>StartCalendarInterval</key>
__CALENDAR__

  <!-- A capture that hangs must not be joined by the next firing. run-all.js also
       holds a pid lock; this is the second line of defence. -->
  <key>AbandonProcessGroup</key>
  <false/>

  <key>StandardOutPath</key>
  <string>__PROJECT_DIR__/logs/launchd.out.log</string>
  <key>StandardErrorPath</key>
  <string>__PROJECT_DIR__/logs/launchd.err.log</string>

  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    <key>QA_NODE_BIN</key>
    <string>__NODE_BIN__</string>
  </dict>

  <key>RunAtLoad</key>
  <false/>
</dict>
</plist>
